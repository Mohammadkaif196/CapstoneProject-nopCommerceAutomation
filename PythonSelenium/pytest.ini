[pytest]
# Only search for tests in the "tests" directory
testpaths = tests

# Only treat files named test_*.py as test files
python_files = test_*.py

# Optional: increase default timeout or add markers
# addopts = -q
